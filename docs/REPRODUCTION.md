# 재현 손실과 단일 통합본 — 2026-09-12 UTC

**최신 단일 ZIP의 새640응답은 .740544 / FP61 / FN23이다.** 이전 새 실행의
.706466보다 +.034078 회복했지만, .751807과 최고 CPU 후보 .758950의 완전한 새
추론 재현은 미완이다. 독립 분수 산술 채점과 현재응답 CPU 재소비가 같은 결과를 냈다.
최고 기록을 지우거나 .740544를 묵시적으로 더 낮은 새 목표로 삼지 않는다.

## 보존본과 최초 하락

보존 응답640개를 동결 CPU 판정기에 다시 넣어 **0.751807 / FP53 / FN24**를
재현했고, CSV도 기존 파일과 바이트 단위로 같았다. 실행 명령은
`tools/replay_preserved_reference.py`; 이 재판정은 약48초, 새 모델 호출0회였다.

최초 개발160건의 새 Gemma 응답640개를 사용한 기록은 **0.706466**이었다.
두 결과의31비트 차이(회복10/새 오류21) 중 새 오류21개는 모두 새 원시 판정에서
이미 발생했다. 기존 보정을 없애거나 누락한 것이 이번 하락의 출발점은 아니다.

## 실제로 같았던 것과 달랐던 것

640개 메시지·명시 SamplingParams·JSON schema, 설치 로그196개 패키지 버전,
모델 경로를 제외한 실제 엔진 설정이 같았다. 배치마다 같은32개 요청을 같은 순서로
넣었다. 보존 토크나이저로 재구성한 입력 토큰도 새 고정 패킷과 일치했다.
단 과거 런타임의 실제 토큰·가중치 파일 해시가 없어 그 바이트까지 직접 증명한 것은 아니다.

**배치 사이의 호출 순서와 엔진 공유 방식은 달랐다.** 과거 A는 공고32개마다
A1→A10→A19를 처리했고, 새 실행은 모든 A1→모든 A10→모든 A19 순서였다.
과거 L19는 별도 엔진의 L1/L10 다음에 생성했지만 새 실행에서는 A 엔진을 공유했다.
캐시 용량과 요청별 캐시 관측에도 차이가 있었다.

## 새384응답의 좁은 순서 대조

같은 A100에서 독립 엔진으로 과거 순서 prefix160과 최근 순서 prefix224를
각각 한 번 실행했다. 원래 입력·32개 묶음·생성 설정·CPU 소비기를 고정했으며
384응답을 모두 회수했다. 재시도0, 형식 오류0이었다.

| 비교 | 과거 순서 | 최근 순서 |
| --- | ---: | ---: |
| 이력이 달라지는96요청에서 과거 native 응답과 완전 일치 | 58 | 13 |
| 같은96요청에서 과거와 다른 원시 판정 비트 수 | 8 | 17 |
| 공통128요청/1,152비트의 최종 오류 | 30 | 32 |

최근 순서는 같은96요청에서 이전 최근 실행의 native 응답89개를 그대로 재현했다.
과거 순서는 최근 순서 대비 최종 오류6개를 회복하고4개를 새로 만들었다.
이는 순서·실행 이력이 무시할 수 없는 변수라는 근거이며,
**새로운 개발160 전체 점수나0.751 전체 복원을 뜻하지 않는다.**

## 남은 한계와 결정

두 엔진에서 같은 첫32요청조차 응답 텍스트5개가 달랐다(원시 비트 차이는0).
실제 KV 캐시 용량도201,624와201,793으로 달랐다. 각 순서를 한 번씩만 실행했으므로
순서만의 효과를 완전히 분리하거나, 남은 변동을 특정 커널 하나로 확정할 수 없다.
중요한 v9 미탐도 이번 순서 복원으로 회복되지 않았다.

따라서 최고 보존본을 폐기하지 않고, **메시지/seed만이 아니라 호출 순서·스케줄링·
캐시 조건까지 재현 계약에 포함**한다. 새 성능 후보는 동일 실행 계약의 새 대조군과
비교해야 한다. 점수가 좋을 때까지 재시도하거나 높은 쪽 응답을 골라 결합하지 않는다.
현재 순서 후보는 자동 생산 채택하지 않았다.

vLLM도 기본 실행에서 재현성을 보장하지 않으며, 오프라인 스케줄링 제어 등 별도
재현 설정을 안내한다. 이것은 이번 관측의 참고 근거이지 단일 원인 증명은 아니다.
[vLLM 0.26.0 재현성 안내](https://docs.vllm.ai/en/v0.26.0/usage/reproducibility/)
의 설정 변경은 위384요청 실험에는 섞지 않았다. 뒤의 canonical640에서 별도로 적용했다.

원본·세부 비교는 비공개로 보존한다. 경로는 `docs/STATE.json`에 연결했다.
root는128개 공통 요청을 독립 재판정해 라벨 확인 전에 동결된 예측과 일치함을 확인했다.
위384요청 원본 회수 후 런타임을 삭제했고, 2026-09-12 13:37:23 UTC에 활성 세션
없음까지 확인했다. 이것은 해당 이전 할당의 종료 기록이다. 이후 아래의 별도
승인된 canonical640 실행과 혼동하지 않는다.

## 단일 ZIP의 실제640응답에서 확인한 것

`script.py` → `submission/`를 유일한 실행 경로로 통합했다. 원래 A 입력 생성기,
보존된 CPU 보정, L19의 v20/e20 연결을 함께 패키징했고, 과거640과 이전 새640의
패킷·CPU 결과가 각각 동일함을 먼저 확인했다. 새 실행에서는 공고32개마다
A1→A10→A19 순서를 복원하고 vLLM import 전에
`VLLM_ENABLE_V1_MULTIPROCESSING=0`을 적용했다. L19는 마지막에 같은 엔진으로
실행하므로, 과거 L의 별도 엔진·선행 L1/L10 이력까지 복원한 것은 아니다.

새640은 재시도0·형식 실패0·결측0으로 종료했다. ZIP과 패킷·실제 토큰640개를
확인했고, 회수된 응답의 CPU 재소비는 원격 CSV와 바이트까지 같다.

| 비교 기준 | 새 통합본 점수 차이 | 회복 / 새 오류 |
| --- | ---: | ---: |
| 최고 CPU 후보 .758950 | -.018406 | 3 / 10 |
| 보존 B4 .751807 | -.011263 | 2 / 9 |
| 이전 새 B4 .706466 | +.034078 | 16 / 12 |

.758950은 별도 v9 응답이 추가된 후보 전체 비교이며 순수 B4 재현 대조군은 아니다.
과거 B4와의 새 오류9개는 모두 원시 판정에서 이미 달랐다. 다른 회복1개는 원시
판정 비트가 아니라 모델 사실 설명의 차이를 기존 CPU 보정이 소비한 경우다.
따라서 이 결과를 보정 누락이나 단일 수치 커널의 문제로 단정하지 않는다.

3회 B3는 .738152, L19 추가160을 쓴 B4는 .740544다. 이 실행에서 L19는 v20 FP를
14→11로 줄였으며 전체 F1 +.002392를 만들었다. 추가 시간은 약255.3초다.
전체160 실행은 약1568.7초(모델 적재312.2초 포함), 같은 A100의1853건 선형
환산은 약4.13시간이다. **L40S2시간을 충족했다는 결과가 아니며 처리량도 미해결이다.**

원본·비교·남은 문제는 `runs/single_submission_20260912/results/`, 독립 채점은
`artifacts/single_submission_20260912/fresh640_independent.json`에 보존한다.
새 GPU 재시도나 새 구현 분기로 해결된 것처럼 처리하지 않는다.

## 후속 0.73 개선을 최고본에 붙이면 얼마인가?

사용자의 지적으로 누락했던 교차 비교를 추가했다. 동일한 v9 G 질문의 실제 저장
응답과 고정 소비기·공백 인용 연결을 두 기준본에 그대로 적용한 결과다.
정답에 따라 적용 공고나 응답을 고르지 않고 고정15공고를 모두 처리했다.

| 기준본 | 변경 전 | 동일 G+공백 연결 후 | 회복 / 새 오류 |
| --- | ---: | ---: | ---: |
| 새640응답 기준 | 0.706466 | 0.730276 | 2 / 1 |
| 과거 최고640응답 기준 | 0.751807 | **0.758950** | 1 / 1 |

새 기준에서 회복한052/v9는 과거 최고본이 이미 맞히던 항목이다.
따라서 새 기준의 상승에는 잃었던 정답을 되찾은 효과가 포함돼 있다.
과거 최고본에선09/v9를 새로 회복하고148/v9 오탐을 추가한다.
0.70→0.73의 상승분을0.75에 단순 가산해0.78로 볼 수 없는 실제 이유다.

이 비교는 새 전체 추론이 아니라 **과거 기준 응답 + 기존 별도 v9 실제 응답의
고정 결합 재판정**이다. 소비기의 fallback만 기준본에 다시 연결했고 모델에 보인
메시지·토큰·원문은 바뀌지 않았다. 새 기준의 네 G/Q·공백 조합은 기존 동결 CSV와
근거까지 일치했다. 과거 기준도 네 조합을 모두 기록했으며 G=.758950,
G+공백=.758950, Q=.755974, Q+공백=.753659였다. 새 GPU 호출은0회다.
기존 동등품 허용 해석의 문제는 남아 있으므로 생산 채택이나 새 추론 최고점으로
승격하지 않는다.

운영상 잘못은 최고본을 보존하는 것으로만 처리하고, 후속 변경을 최고본에 적용한
효과와 재현 손실의 회복을 나누어 보고하지 않은 것이다. 이후 호환 가능한 변경은
반드시 이 두 기준에서 함께 비교한다. 상세는 STATE의 `carryover_comparison`을 따른다.
